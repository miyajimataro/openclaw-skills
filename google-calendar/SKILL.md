# Google Calendar Skill (via gog)

## description
Googleカレンダーに予定を作成する

## commands

### create_event
```bash
gog calendar create primary \
  --summary "{{summary}}" \
  --from "{{from}}" \
  --to "{{to}}"
